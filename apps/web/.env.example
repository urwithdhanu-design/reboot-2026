# Optional frontend Stripe publishable key (also returned by GET /api/payments/config)
# Copy to .env.local — Vite loads it automatically.

VITE_STRIPE_PUBLISHABLE_KEY=pk_test_replace_me
# VITE_API_BASE=

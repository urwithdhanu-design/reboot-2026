# Optional override; default is repo .local-dev/api-target.env (cloud)
# VITE_API_TARGET=cloud
#
# After redeploying Cloud Run, refresh proxy targets:
#   npm run sync:cloud-apis
# Then restart: npm run dev
#
# Platform admin sign-in (KYC service, POST /api/auth/admin/login):
#   Email:    admin@reboot2026.local
#   Password: Reboot2026!Admin   (override in cloud with GCUL_ADMIN_PASSWORD on gcul-kyc)

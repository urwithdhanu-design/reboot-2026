rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Public read-only cache documents written by GCUL services (Admin SDK).
    match /gcul_cache/{docId} {
      allow read: if docId == 'policy_marketplace' || docId.matches('admin_.*');
      allow write: if false;
    }
  }
}
